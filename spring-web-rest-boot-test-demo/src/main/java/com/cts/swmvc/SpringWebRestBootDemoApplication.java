package com.cts.swmvc;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  //original configuration class which includes configuration and spring boot
public class SpringWebRestBootDemoApplication {

	public static void main(String[] args) {
		SpringApplication.run(SpringWebRestBootDemoApplication.class, args);
	}

}
